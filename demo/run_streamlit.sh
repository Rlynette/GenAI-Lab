#!/usr/bin/env bash
# Run streamlit demo (use in Terminal B)
# Assumes virtualenv activated and dependencies installed (see demo/requirements.txt)
streamlit run demo/streamlit_app.py --server.port 8501
