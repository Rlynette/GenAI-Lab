[pytest]
# Only consider tests inside the tests/ directory
testpaths = tests

# Directories pytest should NOT recurse into
norecursedirs =
    tmp_clones
    tmp_clones_*
    tmp_*
    .git
    env
    venv
    .venv
    node_modules
